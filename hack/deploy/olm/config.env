# External Secrets Operator OLM Configuration
# Copy this file and modify the values as needed

export QUAY_USER_ID=ckyal

# Catalog configuration
export CATALOG_NAME=eso-custom-index
export VERSION=1.0.3
export CATALOG_IMG=quay.io/${QUAY_USER_ID}/external-secrets-operator-catalog:v${VERSION}

# Image configuration
export IMAGE_TAG_BASE=quay.io/${QUAY_USER_ID}/external-secrets-operator
export IMG=quay.io/${QUAY_USER_ID}/external-secrets-operator:v${VERSION}

# Deployment configuration
export NAMESPACE=openshift-marketplace
export PUBLISHER="Red Hat Stage Testing"

# Build configuration
export CONTAINER_TOOL=podman
export PLATFORM=linux/amd64
